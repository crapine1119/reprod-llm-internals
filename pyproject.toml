[project]
name = "reprod-llm-embedding"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "accelerate>=1.12.0",
    "jupyter>=1.1.1",
    "matplotlib>=3.10.8",
    "torch>=2.9.1",
    "transformers>=4.57.3",
]
